---
'@smalt-ui/core': patch
---

`SCheckbox` and `SSwitch` now show the `*` marker next to the label when `required` is set, matching `SFormField`-based fields. The accessible name stays the marker-free label text in both.
