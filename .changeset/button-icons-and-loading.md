---
'@smalt-ui/core': minor
---

`SButton` icons follow the button size: 16px for `sm`, 18px for `md` (as before), 20px for `lg`. The loading spinner sits over the content instead of replacing the leading icon, so the button keeps its width when `loading` toggles.
